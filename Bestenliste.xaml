<Window x:Class="Snake_Clon.Bestenliste"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Snake_Clon"
        mc:Ignorable="d"
        Title="Bestenliste" Height="400" Width="250" Topmost="True" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid x:Name="meinGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"></ColumnDefinition>
            <ColumnDefinition Width="2*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Label Grid.ColumnSpan="2" Content="Bestenliste" HorizontalAlignment="Center" FontSize="22 pt" FontWeight="Bold"></Label>
    </Grid>
</Window>
